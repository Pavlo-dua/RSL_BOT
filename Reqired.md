## Завдання на розробку архітектури утиліти автоматизації "Project Raid Automaton"
## 1. Вступ та мета проєкту
Мета: Розробити надійну та розширювану desktop-утиліту для автоматизації рутинних, повторюваних дій (далі — "фарм") у грі "Raid: Shadow Legends". Головна ціль — заощадити час користувача, переклавши монотонні задачі на програму.

Контекст: Гра не надає офіційного API для автоматизації. Тому утиліта повинна взаємодіяти з ігровим клієнтом виключно через візуальний інтерфейс: аналіз зображення на екрані та симуляцію натискань миші.

## 2. Опис предметної області
Архітектор повинен розуміти базові концепції гри:

Фарм (Farming): Це процес багаторазового проходження одного й того ж ігрового рівня (підземелля, кампанії) з метою отримання ресурсів, таких як артефакти (спорядження), досвід або ігрова валюта.

Ресурси: Для виконання дій у грі використовуються обмежені ресурси, що з часом відновлюються. Ключові ресурси — Енергія (для підземель і кампанії) та Ключі Арени (для боїв з іншими гравцями).

Ігровий процес: Весь ігровий процес побудований на переході між різними екранами (меню, лобі, екран бою, екран результатів) через натискання на кнопки.

Цикл фарму: Типовий цикл виглядає так:

Зайти на екран вибору рівня.

Почати бій.

Дочекатися завершення бою (може тривати від 10 секунд до кількох хвилин).

Обробити екран результатів (перемога/поразка, отримані нагороди).

Прийняти рішення щодо нагород (наприклад, продати непотрібний артефакт).

Повернутися на екран вибору рівня для наступного забігу.

## 3. Ключові вимоги та функціонал
Архітектура повинна підтримувати наступний функціонал:

3.1. Ядро взаємодії з UI
Навігація по екранах: Система повинна вміти переходити з будь-якого відомого екрана A на будь-який інший екран B. Навігація має бути побудована на концепції графа станів, де екрани — це вузли, а кнопки — ребра.

Ідентифікація екрана: Утиліта повинна в будь-який момент часу точно визначати, на якому екрані гри вона знаходиться, шляхом пошуку унікальних "контрольних зображень" на екрані.

Взаємодія з елементами: Система повинна знаходити на екрані потрібні елементи (кнопки, іконки) за їхніми графічними шаблонами та симулювати на них клік.

3.2. Сценарії фарму
Концепція "Сценарію": Кожен тип фарму (Арена, Дракон, прокачка героїв) має бути реалізований як окремий, самодостатній "сценарій".

Структура сценарію: Кожен сценарій повинен слідувати загальному шаблону (наприклад, "Шаблонний метод"):

Підготовка: перехід на стартовий екран фарму.

Перевірка умов: перевірка, чи можна продовжувати фарм.

Виконання одного циклу: запуск одного бою/рівня.

Обробка результатів: аналіз екранів після бою, робота з нагородами.

Параметризація сценаріїв: Кожен сценарій повинен приймати об'єкт з налаштуваннями, специфічними для нього. Наприклад:

Сценарій Підземелля: (кількість забігів: 50, продавати артефакти нижче 5 зірок: так).

Сценарій Прокачки: (зупинитись при досягненні MAX рівня: так).

3.3. Гнучка конфігурація
Зовнішні конфігураційні файли: Уся логіка, що може змінитися (описи екранів, шаблони кнопок, параметри сценаріїв, правила продажу), має зберігатися у зовнішніх файлах (наприклад, JSON або XML), а не бути жорстко закодованою в програмі.

Опис екранів (ScreenDefinition): Має існувати універсальна структура для опису будь-якого екрана гри, що включає мапу його ключових елементів (кнопки, індикатори) з їхніми логічними іменами та графічними шаблонами.

## 4. Нефункціональні вимоги
Надійність: 🛡️ Архітектура повинна бути стійкою до ігрових "лагів", несподіваних спливаючих вікон (акції, пропозиції) та помилок. Система повинна вміти відновлювати свою роботу або коректно її завершувати.

Розширюваність: 🧩 Повинно бути максимально просто додати підтримку нового підземелля, нового сценарію чи нового екрана без необхідності змінювати ядро програми. В ідеалі — шляхом додавання нових класів сценаріїв та записів у конфігураційні файли.

Супроводжуваність: 📈 Код має бути чистим, модульним, з чітким розподілом відповідальності між компонентами (навігатор, виконавець сценаріїв, модуль розпізнавання зображень). Оновлення UI в грі має вимагати мінімальних змін у коді.

Продуктивність: 💨 Процес розпізнавання зображень не повинен надмірно навантажувати процесор. Архітектура має передбачати можливості для оптимізації (наприклад, пошук зображення не по всьому екрану, а в конкретній області).

## 5. Технічні обмеження та середовище
Платформа: Windows Desktop.

Технології: остання версія C#, .NET Framework / .NET Core

Метод взаємодії: Виключно аналіз скріншотів екрана та симуляція рухів/кліків миші. Заборонено будь-яке втручання в процеси гри, пам'ять чи мережевий трафік.

## 6. Очікуваний результат
Готова Архітектура утіліти, яка розбита на проєкти, папки і файли, які заповнені основною логікою.
Графічний інтерфейс утіліти написаний на WPF, який взаємодію з користувачем і ядром утіліти. Також цей інтерфейс повинен передбачати використання команд телеграм чата (тобто щоб була можливість віддавати базові команди безпосередньо з чат бота, як то вислати скірншот поточного стану гри, запустити бій на арені з параметрами за замовченням і так далі).
Гра використовує ДіректХ для виводу зображення. Проєкт C:\Home\My_work\RaidBot вже має реалізацію того, щоб робити скрини гри. Ця реалізацуія дуже чорнова, але саме робити скріншоти з гри працють добре.